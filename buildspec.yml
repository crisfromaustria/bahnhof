version: 0.2

phases: 
    install:
        commands:
            - echo "install phase"
            - java -version
            - mvn --version
        finally:
            - echo "install finally"
    pre_build:
        commands: 
            - echo "pre_build phase"
        finally:
            - echo "pre_build finally"
    build:
        commands:
            - echo "build phase BEG"
            - mvn clean package
            - echo "build phase END"
        finally:
            - echo "build finally"
    post_build:
        commands:
            - echo "post_build phase"
            - ls -la
            - pwd
            - ls -la target
            - mv target/bahnhof.jar .
        finally:
            - echo "post_build finally"
artifacts:
    files:
        - "**/*"
